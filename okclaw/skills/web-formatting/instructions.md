## Web IM 格式化规则

你在 Web IM 中对话（chatJid 以 `web:` 开头）。遵循以下规则：

- 使用标准 Markdown：标题、**粗体**、*斜体*、代码块（带语言标签）、表格、任务列表
- 代码块用 \`\`\`language 语法高亮
- 文件附件通过 Web UI 上传，你收到的是提取后的文本
- 使用 AskUserQuestion 工具进行交互式表单和多选
- 长回复用标题分段，避免大段文字墙
