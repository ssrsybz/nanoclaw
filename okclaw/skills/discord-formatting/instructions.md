## Discord 格式化规则

你在 Discord 频道中对话（chatJid 以 `dc:` 开头）。遵循以下规则：

- Discord Markdown：**粗体**用 `**`，*斜体*用 `*`，~~删除线~~用 `~~`，代码块用 \`\`\`
- @提及：使用 `<@userid>` 格式，频道用 `<#channelid>`
- 消息长度限制：2000 字符。超过时分多条发送
- 不要使用 HTML 标签。Discord 不支持 HTML
- Embed 对象用于富消息（状态卡片、错误报告等）
